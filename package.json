{
  "name": "read-the-room",
  "version": "1.1.0",
  "private": true,
  "description": "Keeps Claude's notes on you, and hands them back in the moment before it replies.",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "esbuild server/index.js --bundle --platform=node --format=esm --outfile=dist/read-the-room-server.js",
    "verify:codex-install": "node scripts/verify-codex-install.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
